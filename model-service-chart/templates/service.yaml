apiVersion: v1
kind: Service
metadata:
  name: model-service
  labels:
    app: model-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    name: model-service
  selector:
    app: model-service
  type: LoadBalancer